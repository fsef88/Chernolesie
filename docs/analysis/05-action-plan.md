# План действий по улучшению «Чёрнолесья»

**Цель**: Довести проект до уровня Vampire Survivors по визуальной полировке и качеству.

---

## Приоритет 1 (Критично — сделать в первую очередь)

### 1.1 Исправить orphan-файл Воина
**Файл**: `src/game/data/hero-warrior-v674.orphan.js`

**Проблема**: 16 новых кадров Воина не загружаются, потому что файл лежит вне `<script>`.

**Действие**:
- Перенести содержимое в правильное место в сборке
- Проверить в `src/build.manifest.json`
- После правки запустить `npm run build` и визуально проверить нового Воина

**Ожидаемый эффект**: Воин будет использовать новые кадры.

---

### 1.2 Разбить 3 самых больших файла

#### A. `render/fx-layer.js` (468 строк) — самый приоритетный

**Предлагаемое разделение**:
```
render/
├── fx-layer.js          (главный диспетчер)
├── fx/weapon-trails.js  (следы серпа, косы, молний)
├── fx/particles.js      (частицы, вспышки, орбы)
├── fx/death-effects.js  (взрывы смерти, волны)
└── fx/auras.js          (постоянные ауры оружия)
```

#### B. `entities/enemy-ai.js` (453 строк)

**Предлагаемое разделение**:
```
entities/
├── enemy-ai.js          (главный цикл)
├── enemy/
│   ├── movement.js
│   ├── behavior.js
│   ├── auras.js
│   └── types/           (отдельные файлы на тип врага при необходимости)
```

#### C. `render/props-anomalies.js` (433 строк)

**Предлагаемое разделение**:
```
render/
├── props-anomalies.js
├── props/
│   ├── world-props.js
│   ├── anomalies.js
│   └── decorations.js
```

---

## Приоритет 2 (Высокий — следующие 2–3 недели)

### 2.1 Усилить визуальный Juice

**Что нужно добавить**:

| Фича                              | Где реализовать                  | Сложность |
|-----------------------------------|----------------------------------|---------|
| Крупные титры при эволюции        | `ui/juice.js` + `fx-layer.js`    | Средняя |
| Более яркие и длинные следы оружия| `fx/weapon-trails.js`            | Средняя |
| Эффект взрыва при смерти элиты    | `fx/death-effects.js`            | Средняя |
| Экранная вспышка при синергии     | `cards.js` + `fx-layer.js`       | Лёгкая  |
| Камера-пульс при мощной атаке     | `slowmoHit()` (уже есть)         | Лёгкая  |
| Больше частиц при подборе кристаллов | `pickups.js` + `gemPops`       | Лёгкая  |

### 2.2 Улучшить экран выбора карт

**Текущие проблемы**:
- Слабая анимация появления карт
- Недостаточно визуального различия между редкостями

**Что нужно**:
- Анимация "вылета" карт (как в VS)
- Более яркие рамки и эффекты редкости
- Звуковой feedback при выборе

### 2.3 Улучшить feedback при эволюции и синергии

- Более мощная вспышка экрана
- Крупный текст "ЭВОЛЮЦИЯ!"
- Частицы и звук

---

## Приоритет 3 (Средний — следующие 4–6 недель)

### 3.1 Мобильная оптимизация и полировка

- Увеличить размер кнопок спецатак на мобильных
- Улучшить видимость следов оружия на маленьком экране
- Протестировать на реальных устройствах (средний Android)
- Настроить `partMul` более агрессивно на слабых устройствах

### 3.2 Аудио-juice

- Добавить больше вариаций звуков ударов
- Звуки при получении редкой карты
- Более мощный звук эволюции и синергии

### 3.3 Создать класс `GameState`

**Цель**: Убрать 40+ глобальных переменных.

**Пример структуры**:
```js
class GameState {
  constructor() {
    this.player = new PlayerState();
    this.enemies = [];
    this.weapons = [];
    this.time = 0;
    // ...
  }
  
  reset() { ... }
  serialize() { ... }
  deserialize(data) { ... }
}
```

---

## Приоритет 4 (Долгосрочный)

### 4.1 Добавить тесты

- Smoke-тесты в `tools/check.mjs`
- Проверка ключевых механик (спавн, урон, эволюции)
- Автоматическая проверка при сборке

### 4.2 Оптимизация рендера

- Добавить frustum culling в `fx-layer.js`
- Уменьшить количество `ctx.save/restore`
- Рассмотреть batch rendering для частиц

### 4.3 Новый контент (опционально)

- Новые классы
- Новые синергии
- Новые боссы и мини-боссы

---

## Итоговый план на ближайшие 4 недели

| Неделя | Задачи                                      | Результат |
|--------|---------------------------------------------|---------|
| 1      | Исправить orphan Воина + разбить `fx-layer.js` | Воин работает + чище код |
| 2      | Усилить juice (титры, следы, взрывы)        | Заметно более "вкусная" игра |
| 3      | Улучшить экран карт + feedback эволюций     | Лучше ощущения прогресса |
| 4      | Мобильная полировка + тестирование          | Стабильная работа на телефонах |

---

## Как измерять прогресс

1. **Визуально** — запускать игру и сравнивать с Vampire Survivors
2. **По метрикам**:
   - Время `draw()` < 12 мс
   - Размер `_sepGrid` < 150
   - FPS на среднем телефоне ≥ 55
3. **По отзывам** — попросить 3–5 человек поиграть

---

**Готов начать выполнение плана?**

Напиши:
- **"Начинай с 1.1"** — исправим orphan-файл Воина
- **"Начинай с 1.2A"** — разобьём `fx-layer.js`
- **"Сначала juice"** — начнём усиливать визуальный feedback
- Или предложи свой порядок.